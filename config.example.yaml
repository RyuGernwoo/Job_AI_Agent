chunk_size_chars: 800
chunk_overlap_chars: 120
retrieval_top_k: 5

llm:
  # mock: 로컬 데모/테스트용
  # http_chat: OpenAI-compatible chat completions 형식의 HTTP endpoint용
  provider: mock
  model: lessonpack-mock
  # provider: http_chat
  # model: your-chat-model
  # base_url: https://api.example.com/v1/chat/completions
  # api_key_env: LESSONPACK_HTTP_API_KEY
  # timeout_seconds: 30

vector_store:
  provider: memory
  # provider: chroma
  # persist_path: chroma_db
  # collection_name: lectureops_chunks
