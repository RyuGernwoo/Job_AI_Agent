name: LessonPack AI CD

on:
  workflow_run:
    workflows:
      - LessonPack AI CI
    types:
      - completed
    branches:
      - main
  workflow_dispatch:
    inputs:
      mode:
        description: "Deployment mode"
        required: true
        default: "deploy"
        type: choice
        options:
          - deploy
          - rollback
      image:
        description: "Full image reference for rollback. Empty uses server .previous_image."
        required: false
        type: string

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}/lessonpack-api

jobs:
  resolve:
    name: Resolve Deployment
    runs-on: ubuntu-latest
    outputs:
      mode: ${{ steps.plan.outputs.mode }}
      ref: ${{ steps.plan.outputs.ref }}
      image: ${{ steps.plan.outputs.image }}
      image_repo: ${{ steps.plan.outputs.image_repo }}
      should_build: ${{ steps.plan.outputs.should_build }}
      should_deploy: ${{ steps.plan.outputs.should_deploy }}
    steps:
      - name: Resolve deployment plan
        id: plan
        env:
          EVENT_NAME: ${{ github.event_name }}
          WORKFLOW_RUN_CONCLUSION: ${{ github.event.workflow_run.conclusion }}
          WORKFLOW_RUN_HEAD_SHA: ${{ github.event.workflow_run.head_sha }}
          DISPATCH_MODE: ${{ inputs.mode }}
          DISPATCH_IMAGE: ${{ inputs.image }}
          DEFAULT_REF: ${{ github.sha }}
        run: |
          set -euo pipefail

          mode="${DISPATCH_MODE:-deploy}"
          ref="$DEFAULT_REF"
          should_build="true"
          should_deploy="true"
          image=""

          if [ "$EVENT_NAME" = "workflow_run" ]; then
            mode="deploy"
            ref="$WORKFLOW_RUN_HEAD_SHA"

            if [ "$WORKFLOW_RUN_CONCLUSION" != "success" ]; then
              should_build="false"
              should_deploy="false"
            fi
          fi

          image_repo="$(printf '%s/%s' "$REGISTRY" "$IMAGE_NAME" | tr '[:upper:]' '[:lower:]')"

          if [ "$mode" = "rollback" ]; then
            should_build="false"
            image="$DISPATCH_IMAGE"
          else
            image="${image_repo}:sha-${ref:0:12}"
          fi

          {
            echo "mode=$mode"
            echo "ref=$ref"
            echo "image=$image"
            echo "image_repo=$image_repo"
            echo "should_build=$should_build"
            echo "should_deploy=$should_deploy"
          } >> "$GITHUB_OUTPUT"

  build:
    name: Build & Push to GHCR
    runs-on: ubuntu-latest
    needs: resolve
    if: needs.resolve.outputs.should_build == 'true'
    outputs:
      image: ${{ needs.resolve.outputs.image }}
    steps:
      - name: Checkout deployment source
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.resolve.outputs.ref }}

      - name: Log in to GHCR
        env:
          GHCR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          printf '%s' "$GHCR_TOKEN" | docker login "$REGISTRY" -u "$GITHUB_ACTOR" --password-stdin

      - name: Set up Docker Buildx
        run: |
          set -euo pipefail
          docker buildx create --use
          docker buildx inspect --bootstrap

      - name: Build and push image
        env:
          IMAGE: ${{ needs.resolve.outputs.image }}
          IMAGE_REPO: ${{ needs.resolve.outputs.image_repo }}
        run: |
          set -euo pipefail
          docker buildx build \
            --file Dockerfile \
            --tag "$IMAGE" \
            --tag "${IMAGE_REPO}:main" \
            --cache-from type=gha \
            --cache-to type=gha,mode=max \
            --push \
            .

  deploy:
    name: Deploy to Compute Engine
    runs-on: ubuntu-latest
    needs:
      - resolve
      - build
    if: always() && needs.resolve.outputs.should_deploy == 'true' && (needs.resolve.outputs.should_build == 'false' || needs.build.result == 'success')
    steps:
      - name: Validate deployment secrets
        env:
          GCE_HOST: ${{ secrets.GCE_HOST }}
          GCE_USERNAME: ${{ secrets.GCE_USERNAME }}
          GCE_SSH_KEY: ${{ secrets.GCE_SSH_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          LANGFUSE_PUBLIC_KEY: ${{ secrets.LANGFUSE_PUBLIC_KEY }}
          LANGFUSE_SECRET_KEY: ${{ secrets.LANGFUSE_SECRET_KEY }}
          LANGFUSE_OTEL_HOST: ${{ secrets.LANGFUSE_OTEL_HOST }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          set -euo pipefail
          test -n "$GCE_HOST"
          test -n "$GCE_USERNAME"
          test -n "$GCE_SSH_KEY"
          test -n "$OPENAI_API_KEY"
          test -n "$GEMINI_API_KEY"
          test -n "$LANGFUSE_PUBLIC_KEY"
          test -n "$LANGFUSE_SECRET_KEY"
          test -n "$LANGFUSE_OTEL_HOST"
          test -n "$SUPABASE_URL"
          test -n "$SUPABASE_SERVICE_ROLE_KEY"

      - name: Prepare SSH
        env:
          GCE_HOST: ${{ secrets.GCE_HOST }}
          GCE_SSH_KEY: ${{ secrets.GCE_SSH_KEY }}
        run: |
          set -euo pipefail
          install -m 700 -d "$HOME/.ssh"
          printf '%s\n' "$GCE_SSH_KEY" > "$HOME/.ssh/lessonpack-ai"
          chmod 600 "$HOME/.ssh/lessonpack-ai"
          ssh-keyscan -H "$GCE_HOST" >> "$HOME/.ssh/known_hosts"

      - name: Deploy image over SSH
        env:
          DEPLOY_IMAGE: ${{ needs.resolve.outputs.image }}
          DEPLOY_MODE: ${{ needs.resolve.outputs.mode }}
          GCE_HOST: ${{ secrets.GCE_HOST }}
          GCE_USERNAME: ${{ secrets.GCE_USERNAME }}
          GHCR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GHCR_USERNAME: ${{ github.actor }}
          SERVICE_PORT: ${{ secrets.SERVICE_PORT || '8000' }}
          LESSONPACK_CORS_ALLOW_ORIGINS: ${{ secrets.LESSONPACK_CORS_ALLOW_ORIGINS || 'https://7f62cef5-bc4c-473e-a8d2-5f1847df5736.lovableproject.com,https://id-preview--7f62cef5-bc4c-473e-a8d2-5f1847df5736.lovable.app,https://lessonpack-ai.lovable.app,http://localhost:5173,http://127.0.0.1:5173' }}
          LESSONPACK_CORS_ALLOW_CREDENTIALS: ${{ secrets.LESSONPACK_CORS_ALLOW_CREDENTIALS || 'false' }}
          LESSONPACK_PUBLIC_API_HOST: ${{ secrets.LESSONPACK_PUBLIC_API_HOST || '' }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          LANGFUSE_PUBLIC_KEY: ${{ secrets.LANGFUSE_PUBLIC_KEY }}
          LANGFUSE_SECRET_KEY: ${{ secrets.LANGFUSE_SECRET_KEY }}
          LANGFUSE_OTEL_HOST: ${{ secrets.LANGFUSE_OTEL_HOST }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          LESSONPACK_SUPABASE_TABLE: ${{ secrets.LESSONPACK_SUPABASE_TABLE || 'lessonpack_chunks' }}
          LESSONPACK_SUPABASE_MATCH_FUNCTION: match_lessonpack_chunks_v2
          LESSONPACK_SUPABASE_MATCH_THRESHOLD: ${{ secrets.LESSONPACK_SUPABASE_MATCH_THRESHOLD || '0.0' }}
          LESSONPACK_BASELINE_PROJECT_ID: ${{ secrets.LESSONPACK_BASELINE_PROJECT_ID || 'mvp-dataset' }}
          LESSONPACK_RETRIEVAL_CANDIDATE_K: ${{ secrets.LESSONPACK_RETRIEVAL_CANDIDATE_K || '20' }}
          LESSONPACK_RETRIEVAL_TOP_K: ${{ secrets.LESSONPACK_RETRIEVAL_TOP_K || '5' }}
          LESSONPACK_EMBEDDING_PROVIDER: litellm
          LESSONPACK_EMBEDDING_MODEL: text-embedding-3-small
          LESSONPACK_EMBEDDING_DIMENSIONS: "1536"
          LESSONPACK_SUPABASE_EMBEDDING_COLUMN: embedding_v2
          LESSONPACK_EMBEDDING_VERSION: v2
          LESSONPACK_LITELLM_MODEL: ${{ secrets.LESSONPACK_LITELLM_MODEL || 'gpt-4o-mini' }}
          LESSONPACK_LITELLM_FALLBACK_MODELS: ${{ secrets.LESSONPACK_LITELLM_FALLBACK_MODELS || 'gemini/gemini-2.0-flash' }}
          LESSONPACK_LITELLM_CALLBACKS: ${{ secrets.LESSONPACK_LITELLM_CALLBACKS || 'langfuse_otel' }}
          LESSONPACK_MIN_FREE_DISK_KB: ${{ secrets.LESSONPACK_MIN_FREE_DISK_KB || '1048576' }}
        run: |
          set -euo pipefail

          b64() { printf '%s' "$1" | base64 -w 0; }

          ghcr_token_b64="$(b64 "$GHCR_TOKEN")"
          openai_api_key_b64="$(b64 "$OPENAI_API_KEY")"
          gemini_api_key_b64="$(b64 "$GEMINI_API_KEY")"
          langfuse_public_key_b64="$(b64 "$LANGFUSE_PUBLIC_KEY")"
          langfuse_secret_key_b64="$(b64 "$LANGFUSE_SECRET_KEY")"
          langfuse_otel_host_b64="$(b64 "${LANGFUSE_OTEL_HOST:-}")"
          supabase_url_b64="$(b64 "$SUPABASE_URL")"
          supabase_service_role_key_b64="$(b64 "$SUPABASE_SERVICE_ROLE_KEY")"
          deploy_dir="/home/${GCE_USERNAME}/lessonpack-ai"
          ssh_target="${GCE_USERNAME}@${GCE_HOST}"

          ssh -i "$HOME/.ssh/lessonpack-ai" "$ssh_target" \
            "DEPLOY_DIR='$deploy_dir' DEPLOY_IMAGE='$DEPLOY_IMAGE' DEPLOY_MODE='$DEPLOY_MODE' GHCR_TOKEN_B64='$ghcr_token_b64' GHCR_USERNAME='$GHCR_USERNAME' LANGFUSE_OTEL_HOST_B64='$langfuse_otel_host_b64' LANGFUSE_PUBLIC_KEY_B64='$langfuse_public_key_b64' LANGFUSE_SECRET_KEY_B64='$langfuse_secret_key_b64' GEMINI_API_KEY_B64='$gemini_api_key_b64' OPENAI_API_KEY_B64='$openai_api_key_b64' REGISTRY='$REGISTRY' SERVICE_PORT='$SERVICE_PORT' LESSONPACK_CORS_ALLOW_ORIGINS='$LESSONPACK_CORS_ALLOW_ORIGINS' LESSONPACK_CORS_ALLOW_CREDENTIALS='$LESSONPACK_CORS_ALLOW_CREDENTIALS' LESSONPACK_PUBLIC_API_HOST='$LESSONPACK_PUBLIC_API_HOST' SUPABASE_SERVICE_ROLE_KEY_B64='$supabase_service_role_key_b64' SUPABASE_URL_B64='$supabase_url_b64' LESSONPACK_SUPABASE_TABLE='$LESSONPACK_SUPABASE_TABLE' LESSONPACK_SUPABASE_MATCH_FUNCTION='$LESSONPACK_SUPABASE_MATCH_FUNCTION' LESSONPACK_SUPABASE_MATCH_THRESHOLD='$LESSONPACK_SUPABASE_MATCH_THRESHOLD' LESSONPACK_BASELINE_PROJECT_ID='$LESSONPACK_BASELINE_PROJECT_ID' LESSONPACK_RETRIEVAL_CANDIDATE_K='$LESSONPACK_RETRIEVAL_CANDIDATE_K' LESSONPACK_RETRIEVAL_TOP_K='$LESSONPACK_RETRIEVAL_TOP_K' LESSONPACK_EMBEDDING_PROVIDER='$LESSONPACK_EMBEDDING_PROVIDER' LESSONPACK_EMBEDDING_MODEL='$LESSONPACK_EMBEDDING_MODEL' LESSONPACK_EMBEDDING_DIMENSIONS='$LESSONPACK_EMBEDDING_DIMENSIONS' LESSONPACK_SUPABASE_EMBEDDING_COLUMN='$LESSONPACK_SUPABASE_EMBEDDING_COLUMN' LESSONPACK_EMBEDDING_VERSION='$LESSONPACK_EMBEDDING_VERSION' LESSONPACK_LITELLM_MODEL='$LESSONPACK_LITELLM_MODEL' LESSONPACK_LITELLM_FALLBACK_MODELS='$LESSONPACK_LITELLM_FALLBACK_MODELS' LESSONPACK_LITELLM_CALLBACKS='$LESSONPACK_LITELLM_CALLBACKS' LESSONPACK_MIN_FREE_DISK_KB='$LESSONPACK_MIN_FREE_DISK_KB' bash -s" <<'REMOTE'
          set -euo pipefail

          decode() { printf '%s' "$1" | base64 -d; }

          GHCR_TOKEN="$(decode "$GHCR_TOKEN_B64")"
          OPENAI_API_KEY="$(decode "$OPENAI_API_KEY_B64")"
          GEMINI_API_KEY="$(decode "$GEMINI_API_KEY_B64")"
          LANGFUSE_PUBLIC_KEY="$(decode "$LANGFUSE_PUBLIC_KEY_B64")"
          LANGFUSE_SECRET_KEY="$(decode "$LANGFUSE_SECRET_KEY_B64")"
          LANGFUSE_OTEL_HOST="$(decode "$LANGFUSE_OTEL_HOST_B64")"
          SUPABASE_URL="$(decode "$SUPABASE_URL_B64")"
          SUPABASE_SERVICE_ROLE_KEY="$(decode "$SUPABASE_SERVICE_ROLE_KEY_B64")"

          write_env() {
            image="$1"
            {
              printf 'APP_NAME=lessonpack-ai\n'
              printf 'APP_ENV=production\n'
              printf 'DEBUG=false\n'
              printf 'SERVICE_PORT=%s\n' "$SERVICE_PORT"
              printf 'APP_IMAGE=%s\n' "$image"
              printf 'PYTHONPATH=/app/src\n'
              printf 'LESSONPACK_CONFIG=\n'
              printf 'LESSONPACK_CORS_ALLOW_ORIGINS=%s\n' "$LESSONPACK_CORS_ALLOW_ORIGINS"
              printf 'LESSONPACK_CORS_ALLOW_CREDENTIALS=%s\n' "$LESSONPACK_CORS_ALLOW_CREDENTIALS"
              printf 'LESSONPACK_PUBLIC_API_HOST=%s\n' "$LESSONPACK_PUBLIC_API_HOST"
              printf 'LESSONPACK_LLM_PROVIDER=litellm\n'
              printf 'LESSONPACK_LITELLM_MODEL=%s\n' "$LESSONPACK_LITELLM_MODEL"
              printf 'LESSONPACK_LITELLM_FALLBACK_MODELS=%s\n' "$LESSONPACK_LITELLM_FALLBACK_MODELS"
              printf 'LESSONPACK_LITELLM_TIMEOUT_SECONDS=30\n'
              printf 'LESSONPACK_LITELLM_CALLBACKS=%s\n' "$LESSONPACK_LITELLM_CALLBACKS"
              printf 'OPENAI_API_KEY=%s\n' "$OPENAI_API_KEY"
              printf 'GEMINI_API_KEY=%s\n' "$GEMINI_API_KEY"
              printf 'LANGFUSE_PUBLIC_KEY=%s\n' "$LANGFUSE_PUBLIC_KEY"
              printf 'LANGFUSE_SECRET_KEY=%s\n' "$LANGFUSE_SECRET_KEY"
              printf 'LANGFUSE_OTEL_HOST=%s\n' "$LANGFUSE_OTEL_HOST"
              printf 'LANGFUSE_BASE_URL=%s\n' "$LANGFUSE_OTEL_HOST"
              printf 'LESSONPACK_LANGFUSE_TRACE_NAME=lessonpack-ai-mvp\n'
              printf 'LESSONPACK_LANGFUSE_GENERATION_NAME=lessonpack-ai-generation\n'
              printf 'LESSONPACK_LANGFUSE_SESSION_ID=lessonpack-ai-production\n'
              printf 'LESSONPACK_LANGFUSE_FLUSH_WAIT_SECONDS=1.0\n'
              printf 'LITELLM_LOG=INFO\n'
              printf 'LECTUREOPS_VECTOR_STORE=supabase\n'
              printf 'SUPABASE_URL=%s\n' "$SUPABASE_URL"
              printf 'SUPABASE_SERVICE_ROLE_KEY=%s\n' "$SUPABASE_SERVICE_ROLE_KEY"
              printf 'LESSONPACK_SUPABASE_TABLE=%s\n' "$LESSONPACK_SUPABASE_TABLE"
              printf 'LESSONPACK_SUPABASE_MATCH_FUNCTION=%s\n' "$LESSONPACK_SUPABASE_MATCH_FUNCTION"
              printf 'LESSONPACK_SUPABASE_MATCH_THRESHOLD=%s\n' "$LESSONPACK_SUPABASE_MATCH_THRESHOLD"
              printf 'LESSONPACK_BASELINE_PROJECT_ID=%s\n' "$LESSONPACK_BASELINE_PROJECT_ID"
              printf 'LESSONPACK_RETRIEVAL_CANDIDATE_K=%s\n' "$LESSONPACK_RETRIEVAL_CANDIDATE_K"
              printf 'LESSONPACK_RETRIEVAL_TOP_K=%s\n' "$LESSONPACK_RETRIEVAL_TOP_K"
              printf 'LESSONPACK_EMBEDDING_PROVIDER=%s\n' "$LESSONPACK_EMBEDDING_PROVIDER"
              printf 'LESSONPACK_EMBEDDING_MODEL=%s\n' "$LESSONPACK_EMBEDDING_MODEL"
              printf 'LESSONPACK_EMBEDDING_DIMENSIONS=%s\n' "$LESSONPACK_EMBEDDING_DIMENSIONS"
              printf 'LESSONPACK_SUPABASE_EMBEDDING_COLUMN=%s\n' "$LESSONPACK_SUPABASE_EMBEDDING_COLUMN"
              printf 'LESSONPACK_EMBEDDING_VERSION=%s\n' "$LESSONPACK_EMBEDDING_VERSION"
            } > .env
            chmod 600 .env
          }


          write_caddyfile() {
            if [ -z "${LESSONPACK_PUBLIC_API_HOST:-}" ]; then
              rm -f Caddyfile
              return 0
            fi
            cat > Caddyfile <<CADDY
          ${LESSONPACK_PUBLIC_API_HOST} {
            encode gzip
            reverse_proxy lessonpack-api:8000
          }
          CADDY
          }
          write_compose() {
            cat > docker-compose.yml <<'COMPOSE'
          name: lessonpack-ai

          services:
            lessonpack-api:
              image: ${APP_IMAGE}
              container_name: lessonpack-api
              restart: unless-stopped
              env_file:
                - .env
              environment:
                APP_NAME: ${APP_NAME:-lessonpack-ai}
                APP_ENV: ${APP_ENV:-production}
                PYTHONPATH: /app/src
              ports:
                - "${SERVICE_PORT:-8000}:8000"
              healthcheck:
                test:
                  [
                    "CMD",
                    "python",
                    "-c",
                    "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health', timeout=3).read()",
                  ]
                interval: 30s
                timeout: 5s
                retries: 3
                start_period: 10s
          COMPOSE
            if [ -n "${LESSONPACK_PUBLIC_API_HOST:-}" ]; then
              cat >> docker-compose.yml <<'COMPOSE'

            lessonpack-caddy:
              image: caddy:2.8-alpine
              container_name: lessonpack-caddy
              restart: unless-stopped
              depends_on:
                - lessonpack-api
              ports:
                - "80:80"
                - "443:443"
              volumes:
                - ./Caddyfile:/etc/caddy/Caddyfile:ro
                - caddy_data:/data
                - caddy_config:/config

          volumes:
            caddy_data:
            caddy_config:
          COMPOSE
            fi
          }

          wait_for_health() {
            for attempt in $(seq 1 30); do
              if curl -fsS "http://127.0.0.1:${SERVICE_PORT}/health" > health.json &&
                 curl -fsS "http://127.0.0.1:${SERVICE_PORT}/health/rag" > rag-health.json &&
                 grep -q '"status":"ok"' rag-health.json &&
                 grep -q 'SupabaseVectorStore' rag-health.json &&
                 grep -q 'SupabaseRAGRepository' rag-health.json; then
                cat health.json
                cat rag-health.json
                return 0
              fi
              sleep 2
            done
            return 1
          }

          cleanup_docker_storage() {
            echo "Docker storage before cleanup:"
            docker system df || true
            docker container prune --force || true
            docker image prune --all --force || true
            docker builder prune --all --force || true
            echo "Docker storage after cleanup:"
            docker system df || true
          }

          ensure_free_disk() {
            local storage_path="/var/lib/containerd"
            local available_kb

            if [ ! -d "$storage_path" ]; then
              storage_path="/var/lib/docker"
            fi

            available_kb="$(df -Pk "$storage_path" | awk 'NR == 2 { print $4 }')"
            if [ -z "$available_kb" ]; then
              echo "Could not determine available disk space for $storage_path."
              exit 1
            fi

            echo "Available disk space for $storage_path: ${available_kb}KB (minimum: ${LESSONPACK_MIN_FREE_DISK_KB}KB)"
            if [ "$available_kb" -lt "$LESSONPACK_MIN_FREE_DISK_KB" ]; then
              echo "Insufficient disk space after Docker cleanup. Expand the GCE boot disk or remove non-LessonPack data before retrying."
              exit 1
            fi
          }

          mkdir -p "$DEPLOY_DIR"
          cd "$DEPLOY_DIR"

          old_image="$(cat .current_image 2>/dev/null || true)"

          if [ "$DEPLOY_MODE" = "rollback" ] && [ -z "$DEPLOY_IMAGE" ]; then
            if [ ! -s .previous_image ]; then
              echo "Rollback requested but .previous_image does not exist."
              exit 1
            fi
            DEPLOY_IMAGE="$(cat .previous_image)"
          fi

          if [ -z "$DEPLOY_IMAGE" ]; then
            echo "DEPLOY_IMAGE is required."
            exit 1
          fi

          printf '%s' "$GHCR_TOKEN" | docker login "$REGISTRY" -u "$GHCR_USERNAME" --password-stdin

          write_caddyfile
          write_compose
          write_env "$DEPLOY_IMAGE"

          cleanup_docker_storage
          ensure_free_disk
          docker pull "$DEPLOY_IMAGE"
          docker compose up -d --force-recreate

          if wait_for_health; then
            if [ -n "$old_image" ] && [ "$old_image" != "$DEPLOY_IMAGE" ]; then
              printf '%s\n' "$old_image" > .previous_image
            fi
            printf '%s\n' "$DEPLOY_IMAGE" > .current_image
            cleanup_docker_storage
            docker compose ps
          else
            echo "Health check failed for $DEPLOY_IMAGE."
            docker compose ps || true
            docker logs lessonpack-api --tail 100 || true

            if [ -n "$old_image" ] && [ "$old_image" != "$DEPLOY_IMAGE" ]; then
              echo "Rolling back to $old_image."
              write_env "$old_image"
              docker pull "$old_image" || true
              docker compose up -d --force-recreate
              printf '%s\n' "$old_image" > .current_image
            fi

            exit 1
          fi
          REMOTE

  summary:
    name: Deployment Summary
    runs-on: ubuntu-latest
    needs:
      - resolve
      - build
      - deploy
    if: always()
    steps:
      - name: Write deployment summary
        run: |
          {
            echo "### LessonPack AI CD"
            echo ""
            echo "| Item | Value |"
            echo "| --- | --- |"
            echo "| Mode | ${{ needs.resolve.outputs.mode }} |"
            echo "| Image | ${{ needs.resolve.outputs.image }} |"
            echo "| Build | ${{ needs.build.result }} |"
            echo "| Deploy | ${{ needs.deploy.result }} |"
          } >> "$GITHUB_STEP_SUMMARY"
