# LessonPack AI MVP 검증 프로토콜

작성일: 2026-07-04
대상 서비스: 직업훈련 강의 운영 보조 AI Agent
관련 문서: [`구현명세서.md`](./구현명세서.md), [`데이터셋_선정_계획서.md`](./데이터셋_선정_계획서.md)

---

## 1. 목적

이 문서는 MVP 구현 결과가 실제로 동작하고, 생성 결과가 교재·NCS 근거에 기반하며, 강사가 검수 가능한 품질인지 확인하기 위한 검증 절차를 정의한다.

검증은 다음 4가지를 구분한다.

1. 기능 검증
2. RAG 검색 검증
3. 생성 품질 검증
4. 실증 검증

---

## 2. 검증 원칙

| 원칙 | 설명 |
|---|---|
| 외부 실행 신호 기반 | “잘 될 것 같다”가 아니라 실행 로그와 파일 산출물로 판단 |
| 데이터셋 고정 | 동일한 샘플 데이터셋으로 반복 검증 |
| citation 우선 | 생성물의 근거 연결 여부를 핵심 지표로 둔다 |
| 사람 검수 포함 | 교육 품질은 자동 점수만으로 닫지 않는다 |
| 로그 보존 | 입력, 검색 chunk, 프롬프트, 생성 결과, 수정 이력을 남긴다 |

---

## 3. 검증 환경

| 항목 | 값 |
|---|---|
| OS | Windows 로컬 개발 환경 |
| Python | 3.11 이상 |
| API | FastAPI |
| UI | Streamlit 또는 FastAPI Swagger UI |
| Vector DB | Chroma local persistent store |
| Export | python-docx, 선택 python-pptx |
| 데이터셋 | `데이터셋_선정_계획서.md` 기준 MVP dataset |

실행 파라미터는 `config.yaml`에 명시한다. 코드 내부 암묵 기본값으로 검증하지 않는다.

---

## 4. 검증 단계

### 4.1 Level 0: 정적 검증

| 항목 | 방법 | 통과 기준 |
|---|---|---|
| 설정 파일 | `config.yaml` 확인 | 필수 파라미터 누락 없음 |
| schema | Pydantic model import | import error 없음 |
| 테스트 데이터 | data directory 확인 | raw, processed, gold 존재 |
| 라이선스 | `README_DATASET.md` 확인 | source URL과 license 기록 |

### 4.2 Level 1: 단위 검증

| 모듈 | 검증 항목 | 통과 기준 |
|---|---|---|
| parser_service | PDF/TXT 텍스트 추출 | 빈 문자열이 아니며 source metadata 유지 |
| chunk_service | chunk ID 생성 | 중복 ID 없음 |
| retrieval_service | top-k 검색 | 요청한 k개 이하 반환, metadata 포함 |
| generation_service | JSON 생성 | Pydantic schema 통과 |
| review_service | 상태 전이 | draft→reviewed→approved 정상 |
| export_service | DOCX 생성 | 파일 존재, 주요 섹션 포함 |
| audit_log_service | 로그 저장 | 입력·검색·프롬프트·결과 기록 |

### 4.3 Level 2: 통합 검증

시나리오:

```text
프로젝트 생성
→ 교재 업로드
→ chunking
→ vector DB upsert
→ retrieval
→ generation
→ review
→ export.docx
→ log 확인
```

통과 기준:

- API 요청이 순서대로 성공한다.
- generated package status가 `draft`로 저장된다.
- review 후 status가 `approved`가 된다.
- approved 상태에서 DOCX 파일이 생성된다.
- 로그 파일 또는 DB record가 생성된다.

### 4.4 Level 3: RAG 검색 검증

| 지표 | 측정 방법 | MVP 기준 |
|---|---|---|
| Top-k Hit Rate | gold expected chunk ID가 top-k 안에 있는 비율 | 0.70 이상 |
| ID 기반 Context Precision | retrieved chunk ID와 expected chunk ID 비교 | 0.60 이상 |
| Context Utilization | 생성 결과가 retrieved chunk를 실제 사용했는지 확인 | 핵심 항목 80% 이상 |
| 중복 chunk 비율 | top-k 내 동일 내용 반복 비율 | 20% 이하 |
| 빈 검색률 | 검색 결과가 0개인 query 비율 | 10% 이하 |

Ragas는 RAG 평가용 지표를 제공하며, context precision, context recall, faithfulness, response relevancy 등을 RAG 평가에 활용할 수 있다. 다만 MVP에서는 LLM 기반 평가 점수를 절대값으로 과신하지 않고, ID 기반 검증과 사람 검수를 병행한다.

### 4.5 Level 4: 생성 품질 검증

| 지표 | 측정 방법 | MVP 기준 |
|---|---|---|
| schema 통과율 | Pydantic validation | 100% |
| citation 연결률 | 핵심 생성 항목 중 citation ID 존재 비율 | 90% 이상 |
| 객관식 완성도 | 문항, 보기 4개, 정답, 해설 존재 | 5문항 모두 통과 |
| 실습형 평가 완성도 | 시나리오, 절차, 제출물, 루브릭 존재 | 모두 존재 |
| NCS 연결성 | 생성 항목이 NCS 능력단위와 연결되는지 | 주요 항목 80% 이상 |
| 근거 부족 표시 | 근거 없는 단정 대신 `근거 부족` 표시 | 위반 0건 |

### 4.6 Level 5: 실증 검증

| 항목 | 방법 | 통과 기준 |
|---|---|---|
| 사용자 이해 | 강사/예비 강사 2~3명 사용성 테스트 | 흐름을 설명 없이 수행 가능 |
| 신뢰도 | citation 표시 후 신뢰도 5점 척도 | 평균 4점 이상 목표 |
| 수정 가능성 | 생성 결과 수정·승인 관찰 | 사용자가 수정 가능하다고 판단 |
| 재사용 의향 | “다른 차시에도 써볼 의향” 질문 | 긍정 응답 확보 |
| 작성 시간 체감 | 기존 수작업 대비 비교 질문 | 시간 절감 체감 응답 확보 |

---

## 5. 검증 데이터셋

| 데이터 | 최소 수량 | 용도 |
|---|---:|---|
| 커리큘럼 샘플 | 1개 | 프로젝트 생성 |
| NCS 능력단위 | 1개 이상 | NCS 연결성 검증 |
| 교재 chunk | 30개 이상 | RAG 검색 |
| retrieval gold query | 10개 이상 | 검색 성능 |
| generation gold case | 3개 이상 | 생성 품질 |
| 사용성 테스트 참여자 | 2~3명 | 실증 |

---

## 6. 검증 로그 형식

```json
{
  "run_id": "eval-20260704-001",
  "config": {
    "chunk_size_chars": 800,
    "chunk_overlap_chars": 120,
    "retrieval_top_k": 5,
    "llm_provider": "mock 또는 명시 provider"
  },
  "dataset_version": "mvp-dataset-v0.1",
  "results": {
    "schema_pass_rate": 1.0,
    "citation_coverage": 0.92,
    "top_k_hit_rate": 0.75,
    "docx_export_success": true
  },
  "artifacts": {
    "docx_path": "outputs/docx/package.docx",
    "audit_log_path": "outputs/logs/eval-20260704-001.json"
  }
}
```

---

## 7. 사람 평가 루브릭

각 항목은 1~5점으로 평가한다.

| 평가 항목 | 1점 | 3점 | 5점 |
|---|---|---|---|
| 교안 적합성 | 차시 목표와 거의 무관 | 일부 관련 | 목표와 흐름이 명확히 연결 |
| 실습 적합성 | 수행 불가 | 일부 수정 필요 | 바로 실습 가능 |
| 평가 문항 품질 | 오류 많음 | 기본 구조는 있음 | 정답·해설·난이도 적절 |
| 근거 신뢰도 | 출처 불명확 | 일부 출처 있음 | 주요 내용에 근거 표시 |
| 수정 편의성 | 수정 어려움 | 수정 가능 | 강사가 쉽게 고칠 수 있음 |
| 문서 완성도 | 제출 어려움 | 보완 필요 | 운영 문서로 활용 가능 |

통과 기준:

- 전체 평균 3.5점 이상
- 근거 신뢰도 4점 이상
- 평가 문항 품질 3점 미만이면 재생성 또는 프롬프트 수정

---

## 8. 실패 분류

| 코드 | 분류 | 예시 | 대응 |
|---|---|---|---|
| F-DATA | 데이터 문제 | 교재 텍스트 추출 실패 | TXT 대체 업로드, 파서 교체 |
| F-RET | 검색 문제 | 관련 chunk가 top-k에 없음 | chunk 크기, query 구성 조정 |
| F-GEN | 생성 문제 | JSON schema 위반 | 프롬프트와 Pydantic error feedback 적용 |
| F-CITE | 근거 문제 | citation 누락 | citation_required 검사 강화 |
| F-HITL | 검수 문제 | approved 상태 저장 실패 | 상태 전이 로직 수정 |
| F-EXPORT | 문서화 문제 | DOCX 파일 생성 실패 | export template 수정 |
| F-USR | 사용자 문제 | 사용자가 흐름 이해 못함 | UI 문구·단계 축소 |

---

## 9. 검증 실행 순서

1. 데이터셋 준비 여부 확인
2. 설정 파일 확인
3. parser/chunk 단위 검증
4. retrieval gold query 실행
5. mock LLM으로 schema·workflow 검증
6. 실제 LLM provider 연결
7. generation gold case 실행
8. citation coverage 확인
9. DOCX export 확인
10. 사용성 테스트 2~3명 진행
11. 결과 로그와 실패 분류 작성
12. README와 발표자료에 검증 결과 반영

---

## 10. 완료 판정

MVP 검증 완료 조건:

- 통합 시나리오 1개가 끝까지 성공한다.
- DOCX 산출물이 실제 파일로 생성된다.
- 생성 로그가 남는다.
- citation coverage가 90% 이상이다.
- retrieval top-k hit rate가 70% 이상이다.
- 사람 평가 평균이 3.5점 이상이다.
- 알려진 실패와 한계가 문서에 기록된다.

---

## 11. 참고 자료

- Ragas available metrics: <https://docs.ragas.io/en/stable/concepts/metrics/available_metrics/>
- Ragas context precision: <https://docs.ragas.io/en/stable/concepts/metrics/available_metrics/context_precision/>
- LangGraph human-in-the-loop interrupts: <https://docs.langchain.com/oss/python/langgraph/human-in-the-loop>
- Chroma Getting Started: <https://docs.trychroma.com/docs/overview/getting-started>
- FastAPI Request Files: <https://fastapi.tiangolo.com/tutorial/request-files/>
